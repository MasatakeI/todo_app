export const selectCanPost = (state) => state.todos.canPost;
export const selectIsLoading = (state) => state.todos.isLoading;
export const selectAllTodos = (state) => state.todos.todos;
export const selectError = (state) => state.todos.error;
export const selectIsDeleting = (state) => state.todos.isDeleting;
export const selectIsToggling = (state) => state.todos.isToggling;
